# 꿈꾸는교회 재정관리 포털 E2E 테스트 계획서

**버전**: v2.0 (보안 테스트 추가)  
**작성일**: 2026-05-30 | **최종수정**: 2026-05-30  
**테스트 환경**: http://localhost:3000 (로컬) / https://church-portal-woad.vercel.app (배포)  
**테스트 프레임워크**: Playwright 1.40+  
**테스트 언어**: TypeScript  
**보안 진단 도구**: SecuScanner (자체 개발, `/Users/idaeho/HOME_AUTO/AX-tools/SecuScanner`)

---

## 변경 이력

| 버전 | 날짜 | 변경 내용 |
|------|------|---------|
| v1.0 | 2026-05-30 | 초기 작성 (38 TC) |
| v2.0 | 2026-05-30 | 보안 TC 추가 (TC-SEC-001~031, 13개), 시큐어코딩 검사 절차 추가 |

---

## 1. 테스트 전략

### 1.1 테스트 범위

| 계층 | 범위 | 도구 |
|-----|------|------|
| **E2E** | 전체 사용자 여정 (로그인 → 데이터 입력 → 조회) | Playwright |
| **보안 E2E** | 보안 헤더, 인증/인가, XSS/Injection, TLS | Playwright + SecuScanner |
| **시큐어코딩** | 소스코드 SAST 분석 | SecuScanner SAST 모듈 |
| **취약점 진단** | 배포 사이트 웹 스캔 | SecuScanner 웹 스캔 |
| **통합** | API + DB | - |

### 1.2 우선순위

| 우선순위 | 모듈 | 이유 | 테스트 케이스 수 |
|---------|------|------|----------------|
| **HIGH** | 인증 (로그인/로그아웃) | 가장 기본적이며 다른 테스트의 전제조건 | 5 |
| **HIGH** | 보안 헤더 검증 | 재정 포털 — PII 보호 필수 | 5 |
| **HIGH** | 인증/인가 API 보안 | 미인증 접근 차단 | 6 |
| **HIGH** | 헌금 입력 (`/offerings`) | 핵심 기능 + DB 쓰기 | 6 |
| **HIGH** | 지출 입력 (`/expenses`) | 핵심 기능 + 자동분류 | 6 |
| **MEDIUM** | XSS/Injection 검증 | 사용자 입력 처리 | 2 |
| **MEDIUM** | 페이지 네비게이션 (smoke test) | 404, 리다이렉트 확인 | 11 |
| **MEDIUM** | 피드백 제출 (`/admin`) | 사용자 의견 수집 | 3 |
| **LOW** | TLS/전송 보안 | Vercel 자동 관리 | 2 |
| **LOW** | 데이터 조회 (테이블, 필터링) | 읽기 기능 | 추후 |

### 1.3 테스트 원칙

- **독립성**: 각 테스트는 독립적으로 실행 가능해야 함
- **안정성**: 타이밍 이슈 최소화 → 자동 대기 사용
- **정리**: 테스트 후 생성한 데이터는 DB에서 삭제
- **명확성**: 테스트명으로 테스트할 행동을 명확히 표현

---

## 2. 테스트 케이스 상세 명세

### 2.1 인증 모듈 (`e2e/auth.spec.ts`)

#### TC-AUTH-001: 유효한 자격증명으로 로그인 성공

| 항목 | 설명 |
|-----|------|
| **전제조건** | 애플리케이션이 `/login` 페이지로 접근 가능 |
| **입력** | 이메일: `***REMOVED***`, 비밀번호: `***REMOVED***` |
| **행동** | 1. 이메일 입력란 클릭 후 이메일 입력 <br/> 2. 비밀번호 입력란 클릭 후 비밀번호 입력 <br/> 3. "로그인" 버튼 클릭 |
| **기대결과** | 1. 페이지가 `/dashboard`로 리다이렉트됨 <br/> 2. 대시보드에 사용자명 또는 "안녕하세요" 메시지 표시 <br/> 3. 로그아웃 버튼 가시화 |
| **검증 포인트** | - URL 변경 확인 (`/dashboard` 포함) <br/> - 헤더에 사용자 정보 표시 <br/> - API 호출 성공 (상태 200) |

#### TC-AUTH-002: 잘못된 비밀번호로 로그인 실패

| 항목 | 설명 |
|-----|------|
| **전제조건** | 사용자가 `/login` 페이지에 있음 |
| **입력** | 이메일: `***REMOVED***`, 비밀번호: `wrongpassword123` |
| **행동** | 1. 이메일 및 잘못된 비밀번호 입력 <br/> 2. "로그인" 버튼 클릭 |
| **기대결과** | 1. `/login` 페이지에서 머무름 (리다이렉트 없음) <br/> 2. 에러 메시지 표시: "이메일 또는 비밀번호가 잘못되었습니다" <br/> 3. 입력 필드는 유지됨 |
| **검증 포인트** | - 에러 메시지 텍스트 확인 <br/> - URL이 변경되지 않음 <br/> - form 재입력 가능 |

#### TC-AUTH-003: 존재하지 않는 이메일로 로그인 시도

| 항목 | 설명 |
|-----|------|
| **전제조건** | 사용자가 `/login` 페이지에 있음 |
| **입력** | 이메일: `nonexistent@example.com`, 비밀번호: `anypassword` |
| **행동** | 1. 존재하지 않는 이메일 입력 <br/> 2. "로그인" 버튼 클릭 |
| **기대결과** | 1. 에러 메시지 표시 <br/> 2. `/login` 페이지에 머무름 |
| **검증 포인트** | - 적절한 에러 메시지 표시 |

#### TC-AUTH-004: 로그아웃 성공

| 항목 | 설명 |
|-----|------|
| **전제조건** | 사용자가 로그인된 상태이고 `/dashboard`에 있음 |
| **입력** | 로그아웃 버튼 위치 |
| **행동** | 1. 헤더의 로그아웃 버튼(또는 메뉴) 클릭 <br/> 2. 확인 다이얼로그가 있으면 "확인" 클릭 |
| **기대결과** | 1. `/login` 페이지로 리다이렉트 <br/> 2. 세션 쿠키 삭제됨 <br/> 3. 대시보드에 다시 접근하면 로그인 페이지로 리다이렉트 |
| **검증 포인트** | - URL 변경 확인 <br/> - 세션 유무 확인 |

#### TC-AUTH-005: 비인증 사용자의 보호된 페이지 접근 시 로그인 페이지로 리다이렉트

| 항목 | 설명 |
|-----|------|
| **전제조건** | 사용자가 로그아웃된 상태 |
| **입력** | `/dashboard` URL 직접 접근 |
| **행동** | 1. 주소창에 `http://localhost:3000/dashboard` 입력 <br/> 2. Enter 키 누름 |
| **기대결과** | 1. 자동으로 `/login`으로 리다이렉트 <br/> 2. 로그인 폼 표시 |
| **검증 포인트** | - 리다이렉트 로직 정상 작동 <br/> - 쿠키/세션 검증 |

---

### 2.2 헌금 입력 모듈 (`e2e/offerings.spec.ts`)

#### TC-OFF-001: 헌금 항목 입력 및 저장 성공

| 항목 | 설명 |
|-----|------|
| **전제조건** | 사용자가 로그인되어 `/offerings` 페이지에 있음 |
| **입력** | 날짜: 오늘 날짜, 종류: "십일조", 교인명: "홍길동", 금액: "50000" |
| **행동** | 1. 헌금 입력 폼에서 각 필드 작성 <br/> 2. "저장" 또는 "추가" 버튼 클릭 <br/> 3. 입력 폼이 초기화되거나 새 행이 추가될 때까지 대기 |
| **기대결과** | 1. POST `/api/offerings` 호출 성공 (상태 201 또는 200) <br/> 2. 그리드 또는 테이블에 새 항목 표시 <br/> 3. 토스트 메시지: "저장되었습니다" (또는 유사) <br/> 4. DB에 실제 데이터 저장됨 |
| **검증 포인트** | - API 응답 상태 <br/> - UI에 데이터 즉시 반영 <br/> - DB 쿼리로 데이터 존재 확인 (테스트 후 정리) |
| **정리** | DELETE `/api/offerings/{id}` 호출로 생성된 레코드 삭제 |

#### TC-OFF-002: 필수 필드 누락 시 저장 실패

| 항목 | 설명 |
|-----|------|
| **전제조건** | 사용자가 `/offerings` 페이지에서 새 항목 입력 중 |
| **입력** | 종류: "십일조", 금액: "50000" (교인명 누락) |
| **행동** | 1. 교인명 필드를 비운 상태로 "저장" 버튼 클릭 |
| **기대결과** | 1. POST 요청이 발생하지 않거나 서버가 400 오류 반환 <br/> 2. 교인명 필드에 경고 메시지: "필수 입력입니다" <br/> 3. 입력 필드는 유지됨 |
| **검증 포인트** | - 클라이언트 검증 또는 서버 검증 확인 <br/> - 에러 메시지 명확성 |

#### TC-OFF-003: 자동분류 기능 (헌금 종류 자동 선택)

| 항목 | 설명 |
|-----|------|
| **전제조건** | 사용자가 `/offerings` 페이지에서 헌금 데이터 입력 시 (비고 필드에 특정 키워드) |
| **입력** | 비고: "건축헌금" → 시스템이 자동으로 종류를 "건축"으로 분류 |
| **행동** | 1. 비고 입력란에 "건축헌금" 입력 <br/> 2. 종류 필드에 포커스 이동 또는 저장 버튼 클릭 |
| **기대결과** | 1. 종류 필드가 "건축"으로 자동 선택됨 <br/> 또는 사용자가 수동으로 종류를 선택 가능 |
| **검증 포인트** | - 자동분류 로직 정상 작동 <br/> - 사용자가 수동으로 수정 가능 여부 |
| **비고** | 자동분류 규칙: 비고 필드 키워드 매칭 또는 예측 모델 |

#### TC-OFF-004: 헌금 항목 삭제

| 항목 | 설명 |
|-----|------|
| **전제조건** | 헌금 목록에 하나 이상의 항목이 있음 |
| **입력** | 특정 항목의 "삭제" 버튼 또는 우클릭 메뉴 |
| **행동** | 1. 항목 행의 "삭제" 아이콘/버튼 클릭 <br/> 2. 확인 다이얼로그에서 "삭제" 확인 |
| **기대결과** | 1. DELETE `/api/offerings/{id}` 호출 (상태 200 또는 204) <br/> 2. 그리드에서 항목 제거 <br/> 3. 토스트 메시지: "삭제되었습니다" |
| **검증 포인트** | - API 호출 및 응답 <br/> - UI 즉시 업데이트 <br/> - DB에서 레코드 삭제 확인 |

#### TC-OFF-005: 대량 입력 (엑셀형 그리드)

| 항목 | 설명 |
|-----|------|
| **전제조건** | 사용자가 `/offerings` 페이지에 있으며 그리드 입력 방식 사용 |
| **입력** | 빈 행 3개에 각각 다른 헌금 데이터 입력 |
| **행동** | 1. 그리드의 빈 셀들을 탭/클릭으로 이동하며 데이터 입력 <br/> 2. 각 행을 완성 후 "저장" 또는 엔터 키 <br/> 3. 모든 행 완성 후 전체 "저장" 버튼 클릭 |
| **기대결과** | 1. 각 행이 유효성 검사 통과 <br/> 2. POST `/api/offerings` (bulk) 또는 개별 호출 <br/> 3. 모든 항목이 저장됨 |
| **검증 포인트** | - 그리드 UI 상호작용 <br/> - 유효성 검사 및 저장 로직 |

#### TC-OFF-006: 중복 헌금 입력 방지

| 항목 | 설명 |
|-----|------|
| **전제조건** | 특정 헌금 항목(날짜, 교인, 금액이 동일)이 이미 저장됨 |
| **입력** | 같은 항목을 다시 입력하여 저장 시도 |
| **행동** | 1. 동일한 데이터 입력 후 "저장" 클릭 |
| **기대결과** | 1. 경고 메시지: "이미 등록된 헌금입니다" 또는 <br/> 2. 서버가 409 Conflict 반환 <br/> 3. 사용자가 수정하거나 취소할 수 있음 |
| **검증 포인트** | - 중복 검증 로직 <br/> - 사용자 알림 |

---

### 2.3 지출 입력 모듈 (`e2e/expenses.spec.ts`)

#### TC-EXP-001: 지출 항목 입력 및 저장 성공

| 항목 | 설명 |
|-----|------|
| **전제조건** | 사용자가 로그인되어 `/expenses` 페이지에 있음 |
| **입력** | 날짜: 오늘, 계정과목: "사례비", 내용: "목사님 월급", 금액: "500000" |
| **행동** | 1. 각 입력 필드에 데이터 입력 <br/> 2. "저장" 버튼 클릭 <br/> 3. 저장 완료 대기 |
| **기대결과** | 1. POST `/api/expenses` 호출 성공 (상태 201 또는 200) <br/> 2. 그리드/테이블에 새 항목 표시 <br/> 3. 토스트 메시지 표시 <br/> 4. 입력 폼 초기화 |
| **검증 포인트** | - API 응답 <br/> - UI 업데이트 <br/> - DB에 데이터 저장 |
| **정리** | DELETE `/api/expenses/{id}` 호출로 생성된 데이터 삭제 |

#### TC-EXP-002: 계정과목 자동분류

| 항목 | 설명 |
|-----|------|
| **전제조건** | 사용자가 `/expenses` 페이지에서 지출 입력 중 |
| **입력** | 내용: "사무용 볼펜 구매" → 시스템이 자동으로 계정과목을 "사무비"로 분류 |
| **행동** | 1. 내용 필드에 "사무용 볼펜 구매" 입력 <br/> 2. 계정과목 필드에 포커스 이동 또는 저장 <br/> 3. 자동 분류 결과 확인 |
| **기대결과** | 1. 계정과목 필드가 "사무비"로 자동 선택되거나 <br/> 2. AI 또는 규칙 기반 제안 표시 <br/> 3. 사용자는 수동으로 변경 가능 |
| **검증 포인트** | - 자동분류 정확도 <br/> - 사용자 수동 변경 가능 여부 |

#### TC-EXP-003: 지출 항목 삭제

| 항목 | 설명 |
|-----|------|
| **전제조건** | 지출 목록에 하나 이상의 항목이 있음 |
| **입력** | 특정 항목의 "삭제" 버튼 |
| **행동** | 1. 항목의 삭제 버튼 클릭 <br/> 2. 확인 대화상자에서 "삭제" 선택 |
| **기대결과** | 1. DELETE `/api/expenses/{id}` 호출 <br/> 2. 항목이 그리드에서 제거 <br/> 3. 토스트 메시지 표시 |
| **검증 포인트** | - API 호출 <br/> - UI 업데이트 |

#### TC-EXP-004: 필수 필드 검증

| 항목 | 설명 |
|-----|------|
| **전제조건** | 사용자가 `/expenses` 페이지에 있음 |
| **입력** | 날짜만 입력하고 나머지 필드 비운 상태 |
| **행동** | 1. "저장" 버튼 클릭 |
| **기대결과** | 1. 필수 필드에 오류 표시 <br/> 2. 저장되지 않음 |
| **검증 포인트** | - 클라이언트 검증 <br/> - 에러 메시지 |

#### TC-EXP-005: 지출 필터링 (계정과목별)

| 항목 | 설명 |
|-----|------|
| **전제조건** | `/expense-view` 페이지에 다양한 계정과목의 지출 데이터 있음 |
| **입력** | 필터 드롭다운에서 "사례비" 선택 |
| **행동** | 1. 필터 드롭다운 클릭 <br/> 2. "사례비" 선택 |
| **기대결과** | 1. 테이블이 "사례비"로 필터링됨 <br/> 2. 다른 계정과목의 항목은 숨겨짐 <br/> 3. 합계 값이 업데이트됨 |
| **검증 포인트** | - 필터링 로직 <br/> - 테이블 재렌더링 |

#### TC-EXP-006: 지출 합계 계산

| 항목 | 설명 |
|-----|------|
| **전제조건** | 여러 지출 항목이 저장됨 |
| **입력** | 페이지 로드 또는 필터 변경 |
| **행동** | 1. `/expense-view` 페이지 로드 <br/> 2. 테이블 하단의 합계 행 확인 |
| **기대결과** | 1. 합계 금액이 올바르게 계산됨 <br/> 2. 필터 적용 시 합계 재계산 |
| **검증 포인트** | - 계산 정확도 <br/> - 통화 포맷팅 |

---

### 2.4 전체 페이지 네비게이션 (Smoke Test) (`e2e/pages.spec.ts`)

각 페이지에 대해 다음을 검증:

| 페이지 | URL | 검증 항목 |
|-------|-----|---------|
| **홈/랜딩** | `/` | 로그인 폼 또는 대시보드 (인증 상태에 따라) |
| **로그인** | `/login` | 로그인 폼, "로그인" 버튼, 404 아님 |
| **대시보드** | `/dashboard` | KPI 카드, 수입/지출 차트, 404 아님 |
| **헌금 입력** | `/offerings` | 그리드/폼, "저장" 버튼, 404 아님 |
| **지출 입력** | `/expenses` | 그리드/폼, "저장" 버튼, 404 아님 |
| **수입내역** | `/income` | 테이블, 필터, 404 아님 |
| **지출내역** | `/expense-view` | 테이블, 필터, 합계, 404 아님 |
| **헌금대장** | `/account-book` | 교인별 집계, 검색, 404 아님 |
| **주간결산** | `/weekly-report` | 주차 선택, 요약 데이터, 404 아님 |
| **통장관리** | `/account-mgmt` | 계좌 목록, 잔액, 404 아님 |
| **관리자** | `/admin` | 피드백 관리, 비밀번호 변경, 404 아님 |

#### TC-PAGE-001 ~ TC-PAGE-011: 페이지별 네비게이션 및 404 확인

| 항목 | 설명 |
|-----|------|
| **전제조건** | 사용자가 로그인되어 있음 (보호된 페이지의 경우) |
| **입력** | 각 페이지 URL |
| **행동** | 1. 페이지 로드 <br/> 2. HTTP 상태 코드 확인 <br/> 3. 주요 요소 존재 여부 확인 |
| **기대결과** | 1. HTTP 200 응답 <br/> 2. 404 에러 없음 <br/> 3. 페이지 제목/헤더 표시 <br/> 4. 주요 콘텐츠 렌더링 |
| **검증 포인트** | - HTTP 상태 코드 <br/> - DOM 요소 가시성 <br/> - 로드 시간 < 3초 |

---

### 2.5 피드백 모듈 (`e2e/feedback.spec.ts`)

#### TC-FB-001: 피드백 제출 성공

| 항목 | 설명 |
|-----|------|
| **전제조건** | 사용자가 로그인되어 `/admin` 또는 피드백 양식에 접근 가능 |
| **입력** | 제목: "대시보드 개선", 내용: "월별 비교 차트가 있으면 좋겠습니다" |
| **행동** | 1. 피드백 입력 폼 작성 <br/> 2. "제출" 버튼 클릭 |
| **기대결과** | 1. POST `/api/feedback` 호출 성공 (상태 201) <br/> 2. 토스트 메시지: "피드백이 전송되었습니다" <br/> 3. 폼 초기화 |
| **검증 포인트** | - API 호출 및 응답 <br/> - UI 피드백 |
| **정리** | DB에서 테스트 피드백 삭제 |

#### TC-FB-002: 필수 필드 검증

| 항목 | 설명 |
|-----|------|
| **전제조건** | 사용자가 피드백 폼에 있음 |
| **입력** | 제목만 입력하고 내용 비운 상태 |
| **행동** | 1. "제출" 버튼 클릭 |
| **기대결과** | 1. 내용 필드에 오류 메시지 <br/> 2. 제출되지 않음 |
| **검증 포인트** | - 유효성 검사 |

#### TC-FB-003: 관리자 피드백 목록 조회

| 항목 | 설명 |
|-----|------|
| **전제조건** | 관리자가 `/admin` 페이지에서 피드백 섹션 접근 |
| **입력** | 페이지 로드 |
| **행동** | 1. `/admin` 페이지 로드 <br/> 2. 피드백 테이블/목록 확인 |
| **기대결과** | 1. 제출된 피드백이 목록에 표시됨 <br/> 2. 각 항목에 제목, 내용, 날짜 표시 |
| **검증 포인트** | - GET `/api/feedback` 호출 <br/> - 데이터 렌더링 |

---

## 3. 테스트 환경 구성

### 3.1 필수 라이브러리 설치

```bash
npm install -D @playwright/test
```

### 3.2 환경 변수 설정

로컬 테스트: `.env.test` 파일 사용  
CI/CD: GitHub Actions에서 시크릿 설정

```bash
# .env.test
PLAYWRIGHT_TEST_BASE_URL=http://localhost:3000
TEST_EMAIL=***REMOVED***
TEST_PASSWORD=***REMOVED***
```

### 3.3 로컬 테스트 실행

```bash
# 모든 테스트 실행
npm run test:e2e

# 특정 파일만 실행
npm run test:e2e -- auth.spec.ts

# UI 모드 (시각적 디버깅)
npm run test:e2e -- --ui

# 특정 브라우저만 실행
npm run test:e2e -- --project=chromium
```

---

## 4. Flaky Test 관리

### 4.1 Flaky Test 식별 기준

- 같은 테스트를 5회 반복 실행 중 1회 이상 실패
- 타이밍 이슈 (네트워크, 애니메이션)
- 데이터베이스 동시성 문제

### 4.2 Flaky Test 격리

```typescript
test.fixme(
  browser === 'firefox',
  'FLAKY on Firefox - Issue #123'
)

test.skip(
  isCI && date > '2026-06-30',
  'Skipped temporarily - waiting for DB fix'
)
```

### 4.3 일반적인 원인 및 해결법

| 원인 | 증상 | 해결법 |
|-----|------|-------|
| 네트워크 타이밍 | 간헐적 API 타임아웃 | `waitForResponse()`, `waitForLoadState('networkidle')` |
| 애니메이션 | 클릭 전 요소 이동 | `waitForSelector()` 또는 `isVisible()` 확인 |
| 데이터 경합 | 동시 쓰기 충돌 | 테스트마다 고유 데이터 사용 (timestamp 포함) |
| 브라우저 차이 | Safari에서만 실패 | `test.skip(browserName === 'webkit', '...')` |

---

## 5. CI/CD 통합

### 5.1 GitHub Actions 설정 (`.github/workflows/e2e.yml`)

```yaml
name: E2E Tests
on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '18'
      - run: npm ci
      - run: npm run build
      - run: npx playwright install --with-deps
      - run: npm run test:e2e
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: playwright-report
          path: test-results/
```

### 5.2 실패 시 아티팩트 수집

- 스크린샷: `test-results/artifacts/`
- 비디오: `test-results/artifacts/`
- 트레이스: `test-results/artifacts/`

---

## 6. 성공 기준

| 메트릭 | 목표 | 검증 방법 |
|-------|------|---------|
| **전체 패스율** | 100% | CI 파이프라인 |
| **Flaky 비율** | < 5% | 10회 반복 실행 |
| **테스트 기간** | < 10분 | CI 로그 |
| **아티팩트 업로드** | 100% (실패 시) | GitHub Actions |
| **커버리지** | 모든 주요 사용자 여정 | 테스트 케이스 추적 |

---

## 7. 테스트 실행 절차

### 7.1 로컬 개발 중

```bash
# 1. 개발 서버 시작 (다른 터미널에서)
npm run dev

# 2. 테스트 실행 (watch 모드)
npm run test:e2e -- --watch

# 3. 실패 시 디버그
npm run test:e2e -- --debug
```

### 7.2 커밋 전

```bash
# 1. 모든 테스트 실행 (CI와 동일한 환경)
npm run test:e2e

# 2. 리포트 확인
npm run test:e2e -- --reporter=html
npx playwright show-report
```

### 7.3 배포 전

```bash
# 1. 전체 테스트 5회 반복 (flaky 검증)
npm run test:e2e -- --repeat-each=5

# 2. 모든 브라우저에서 실행
npm run test:e2e
```

---

## 8. 문서 및 참고

### 8.1 테스트 파일 위치

```
/Users/idaeho/MACOS_AUTO/church-portal/
├── e2e/
│   ├── auth.spec.ts          # TC-AUTH-001 ~ 005
│   ├── offerings.spec.ts      # TC-OFF-001 ~ 006
│   ├── expenses.spec.ts       # TC-EXP-001 ~ 006
│   ├── pages.spec.ts          # TC-PAGE-001 ~ 011 (smoke test)
│   └── feedback.spec.ts       # TC-FB-001 ~ 003
├── playwright.config.ts
├── .env.test
└── docs/E2E_TEST_PLAN.md      # 본 문서
```

### 8.2 package.json 스크립트

```json
{
  "test:e2e": "playwright test",
  "test:e2e:debug": "playwright test --debug",
  "test:e2e:ui": "playwright test --ui",
  "test:e2e:headed": "playwright test --headed"
}
```

### 8.3 외부 링크

- [Playwright 공식 문서](https://playwright.dev)
- [Best Practices](https://playwright.dev/docs/best-practices)
- [디버깅 가이드](https://playwright.dev/docs/debug)

---

## 9. 보안 테스트 절차 (v2 신규)

### 9.1 SecuScanner 배포 사이트 스캔

```bash
# 실행
cd /Users/idaeho/HOME_AUTO/AX-tools/SecuScanner
bash run_scan.sh "https://church-portal-woad.vercel.app/"

# 결과 저장 위치
reports/church-portal-woad.vercel.app_{timestamp}.html
reports/church-portal-woad.vercel.app_{timestamp}.json
```

**주기**: 배포 후 매회, 월 1회 정기 스캔

**목표 등급**: D (현재) → B 이상 (보안 헤더 조치 후)

### 9.2 SAST 소스코드 시큐어코딩 검사

```bash
# SAST 서브커맨드 실행
cd /Users/idaeho/HOME_AUTO/AX-tools/SecuScanner
python -m secu_scanner.cli sast /Users/idaeho/MACOS_AUTO/church-portal --out reports

# 의존성 취약점 검사
python -m secu_scanner.cli deps /Users/idaeho/MACOS_AUTO/church-portal --out reports

# 시크릿 누출 검사
python -m secu_scanner.cli secrets /Users/idaeho/MACOS_AUTO/church-portal --out reports
```

**점검 항목**:
| 분류 | 항목 |
|------|------|
| 인증 | 하드코딩된 비밀번호, API 키 노출 |
| 입력값 | SQL Injection, XSS 취약 패턴 |
| 암호화 | 약한 알고리즘 사용 (MD5, SHA1) |
| 의존성 | CVE 취약점 있는 패키지 |
| 설정 | 디버그 모드, 오류 메시지 노출 |

### 9.3 E2E 보안 테스트 실행

```bash
# 보안 TC만 실행
npm run test:e2e -- e2e/security.spec.ts

# 배포 환경 대상
BASE_URL=https://church-portal-woad.vercel.app npm run test:e2e -- e2e/security.spec.ts
```

### 9.4 SecuScanner 최초 스캔 결과 (2026-05-30)

| ID | 등급 | 제목 | 조치 |
|----|------|------|------|
| H-02 | HIGH | CSP 헤더 누락 | ✅ next.config.mjs 추가 |
| H-03 | MEDIUM | X-Frame-Options 부재 | ✅ next.config.mjs 추가 |
| H-04 | LOW | X-Content-Type-Options 부재 | ✅ next.config.mjs 추가 |
| H-05 | LOW | Referrer-Policy 부재 | ✅ next.config.mjs 추가 |
| H-06 | LOW | Permissions-Policy 부재 | ✅ next.config.mjs 추가 |
| T-03 | INFO | 인증서 만료 2026-07-27 | ⚠️ Vercel 자동갱신 모니터링 |
| T-INFO | INFO | TLSv1.3, AES-128-GCM | ✅ 양호 |
| PII-01 | HIGH | DB 개인정보 평문 저장 | 🔧 DESIGN_v2.md 설계 완료 |

---

## 10. 추후 확장 계획

### Phase 2 (v2.1)

- [ ] PII 암호화 구현 후 TC-SEC-040~045 추가 (암호화 검증)
- [ ] API 통합 테스트 (Mock 데이터)
- [ ] 접근성 테스트 (WCAG 2.1 Level A)
- [ ] SecuScanner 재스캔 — 등급 B 이상 확인

### Phase 3 (v3.0)

- [ ] CI/CD 파이프라인에 SecuScanner 자동 통합
- [ ] 시각 회귀 테스트 (스크린샷 비교)
- [ ] 성능 테스트 (LCP, FID, CLS)
- [ ] 감사 로그 E2E 검증

---

**버전**: 2.0  
**작성자**: Claude Code  
**마지막 수정**: 2026-05-30  
**연관 문서**: DESIGN_v2.md, e2e/security.spec.ts

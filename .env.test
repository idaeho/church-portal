# E2E Test Environment Variables
PLAYWRIGHT_TEST_BASE_URL=http://localhost:3000

# Test Credentials (값은 환경변수 또는 CI Secret으로 주입 — 절대 여기에 평문 저장 금지)
TEST_EMAIL=
TEST_PASSWORD=

# Database (placeholder - use real test DB URL in CI)
DATABASE_URL=postgresql://test:test@localhost:5432/church_portal_test

# NextAuth Configuration (test mode)
NEXTAUTH_SECRET=test-secret-key-for-e2e-testing-only
NEXTAUTH_URL=http://localhost:3000
